apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enclave.fullname" . }}-test-connection"
  labels:
    {{- include "enclave.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enclave.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
